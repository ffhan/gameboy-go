package go_gb

const (
	DIV  uint16 = 0xFF04 // Divider register (R/W)
	TIMA uint16 = 0xFF05 // Timer counter (R/W)
	TMA  uint16 = 0xFF06 // Timer modulo (R/W)
	TAC  uint16 = 0xFF07 // Timer control (R/W)
)
